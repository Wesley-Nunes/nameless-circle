import { describe, it } from 'vitest'

// import newParty from './newParty'

// import type { Hero } from 'game/types'

// TODO: Recreate the tests when more hero characters are available.
describe.skip('newParty', () => {
    it.skip('successfully adds valid hero to party', () => {})
    it.skip('throws when old party size is invalid', () => {})
    it.skip('throws when new hero ID is invalid', () => {})
    it.skip('throws when new party size becomes invalid', () => {})
    it.skip('does not mutate original party', () => {})
    it.skip('handles adding to empty party', () => {})
})
