import { describe, expect, it } from 'vitest'

import findTarget from './findTarget'

import type { Hero, Enemy } from 'game/types'

describe('findTarget', () => {
    it('should target weakest enemy', () => {})
    it.todo('should target strongest hero', () => {})
    it.todo('should ignore dead characters', () => {})
    it.todo('should handle single valid target', () => {})
    it.todo('should return first target when equal HP', () => {})
    it.todo('should throw error for invalid team', () => {})
})
